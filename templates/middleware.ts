/**
 * Auto-written by orione-content-link on npm install.
 * `config` must be declared inline here — Next.js forbids re-exporting it.
 */
export { middleware } from "orione-content-link"

export const config = {
  matcher: ["/c/:code*"],
}
